### CSS LAYOUT & POSITIONING ###

    # Define how elements are arranged on a web page.
    # Display properties (block, inline, flex, grid)

# Positioning elements (static, relative, absolute, fixed, sticky)
# Alignment and spacing (top, right, bottom, left, z-index)
# Overflow handling
# The dislpay property defines how an element behaves in layout.

# BLOCK VS INLINE ELEMENTS
    # Block starts on a new line, takes full width, and examples that have default block are <div>, <p>, <h1>, <section>.
    # Inline styas inline and only takes needed width.

    # Examples of default inline include: <span>, <a>, and <strong>.

# POSITIONING

    # Static (default: Normal flow, no positioning).
    # Relative: moves relative to its normal position.
    # Absolute: moves relative to the nearest positioned ancestor.
    # Fixed: stays fixed on the screen.
    # Sticky: sticks when scrolling past a point.

# CONTROLLING ELEMENT PLACEMENT

    # Top, right, bottom, left
    # push off from right, push off from bottom, push off from left.

# HANDLING OVERFLOW

    # Overflow property controls the content inside a container.
        # visible (default)
        # content overflows
        # hidden (extra conent hidden)
        # scrolls (adds scroll bars)
        # auto (a)dds scroll bars when needed)